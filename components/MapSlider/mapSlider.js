import style from './mapSlider.module.scss'
import { useState } from 'react'

const MapSlider = () => {
/*   // set map slider initial state to false - onClick === true
  const [click, setClick] = useState(false);
  // function for setting new state on click
  const handleClick = () => setClick(!click); */

  return (
    <div className={style.container}>
    

    </div>
  )

}

export default MapSlider